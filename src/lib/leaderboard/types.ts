export type Member = {
  id: number;
  name: string;
  domain: string;
  totalPoints: number;
};

export type LeaderboardEntry = {
  rank: number;
  id: number;
  name: string;
  domain: string;
  totalPoints: number;
};